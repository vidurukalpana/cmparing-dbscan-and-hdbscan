# cmparing-dbscan-and-hdbscan
This repository is used to compare DBSCAN and HDBSCAN clustering algorithms. DBSCAN and HDBSCAN are both unsupervised clustering algorithms that group points based on density rather than assuming predefined cluster shapes.
